<view class="map_container">
     <map id="myMap"  show-location longitude="{{longitude}}" latitude="{{latitude}}" controls="{{controls}}" markers="{{markers}}"  include-points="{{markers}}" bindcontroltap="controltap"></map> 
</view>
