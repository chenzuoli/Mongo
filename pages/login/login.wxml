<!--pages/login/login.wxml-->
<script type="text/javascript">
  <button type="default" size="{{defaultSize}}" loading="{{loading}}" plain="{{plain}}"
          disabled="{{disabled}}" bindtap="default" hover-class="other-button-hover"> default </button>
  <button type="primary" size="{{primarySize}}" loading="{{loading}}" plain="{{plain}}"
          disabled="{{disabled}}" bindtap="primary"> primary </button>
  <button type="warn" size="{{warnSize}}" loading="{{loading}}" plain="{{plain}}"
          disabled="{{disabled}}" bindtap="warn"> warn </button>
  <button bindtap="setDisabled">点击设置以上按钮disabled属性</button>
  <button bindtap="setPlain">点击设置以上按钮plain属性</button>
  <button bindtap="setLoading">点击设置以上按钮loading属性</button>
  <button open-type="contact">进入客服会话</button>
  <button open-type="getUserInfo" lang="zh_CN" bindgetuserinfo="onGotUserInfo">获取用户信息</button>
  <button open-type="getUserLocation" lang="zh_CN" bindtap="onReady">获取位置信息</button>

<!-- <button open-type="navigate" url="/page/userInfo/userInfo?title=navigate" hover-class="navigator-hover">跳转到新页面</button> -->
<!-- // navigator 组件默认的 open-type 为 navigate  -->
<!-- <navigator url="/page/userInfo/userInfo?title=navigate" hover-class="navigator-hover">跳转到新页面</navigator> -->
  <button bindtap="navigatePage">跳转到用户信息页</button>


</script>
